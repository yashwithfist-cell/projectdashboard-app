# ===================================================================
# DATABASE CONFIGURATION
# Uses environment variables when available (in Docker), otherwise uses
# the default values provided after the colon for local development.
# ===================================================================
spring.datasource.url=${SPRING_DATASOURCE_URL}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}


#spring.datasource.url=jdbc:postgresql://localhost:5432/empmansys
#spring.datasource.username=postgres
#spring.datasource.password=root


# ===================================================================
# JPA / HIBERNATE CONFIGURATION
# ===================================================================
# On first run, 'update' will create the database schema from your entities.
spring.jpa.hibernate.ddl-auto=update

# Good for debugging, shows the SQL queries being run.
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
# The hibernate.dialect property is no longer needed; Spring Boot auto-detects it.


# ===================================================================
# SERVER CONFIGURATION
# ===================================================================
server.port=8080
# Binds the server to all network interfaces, essential for Docker and network access.
server.address=0.0.0.0


# ===================================================================
# OPTIONAL CONFIGURATIONS
# ===================================================================
# File Upload Limits
spring.servlet.multipart.max-file-size=40MB
spring.servlet.multipart.max-request-size=40MB

# Detailed Logging for Debugging
logging.level.org.springframework.security=DEBUG
logging.level.org.hibernate.SQL=DEBUG

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=yashwithkumar3@gmail.com
spring.mail.password=uodw qxnw lrqq tjmp
spring.mail.protocol=smtp
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.test-connection=false


# SOAP Config
etimetrack.soap.endpoint=http://192.168.1.254:97/WebAPIService.asmx
etimetrack.soap.action=http://tempuri.org/GetTransactionsLog

#Biometric Configurations
biometric.username=Fist$1234
biometric.password=Fist$1234
biometric.device.serial=CUB7243500277


spring.data.web.pageable.default-page-size=10
spring.data.web.pageable.max-page-size=50
